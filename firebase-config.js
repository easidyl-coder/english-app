/* ── 로그인·진도 저장 설정 ──
   아직 비어 있으면(null) 로그인 없이 기기 안에만 저장합니다.
   Firebase 프로젝트를 만든 뒤, Firebase가 보여주는 설정값을 아래처럼 채워 넣으면 됩니다.

   window.FIREBASE_CONFIG = {
     apiKey: "....",
     authDomain: "....firebaseapp.com",
     projectId: "....",
     appId: "...."
   };
*/
window.FIREBASE_CONFIG = null;
